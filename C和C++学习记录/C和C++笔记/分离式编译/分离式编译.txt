function.cpp写函数定义
head.h写宏定义，函数声明，
main.cpp主函数写函数调用，
后两者必要时引入头文件

可以将类的声明放在头文件
类的函数实现放在一个cpp
类的使用实例化放在主函数cpp；
将方法声明放在头文件
方法定义放在cpp；
也可以将类整个声明和定义放在头文件
也可以将整个类的声明和定义放在cpp包括放在
主函数cpp；

大多数编译器要求:类模板的声明与实现放同一个头文件；
