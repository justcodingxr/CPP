函数的声明或者定义必须出现在调用之前；

普通函数的定义在main之外；
普通函数的声明可以在main内或外；

成员函数的声明必须在类的内部；
成员函数的定义可以在类的内部或者外部

可以将类的声明放在头文件
类的函数实现放在一个cpp
类的使用实例化放在主函数cpp；
将方法声明放在头文件
方法定义放在cpp；
也可以将类整个声明和定义放在头文件
也可以将整个类的声明和定义放在cpp包括放在
主函数cpp；
